export const Button = ({children}) => {
  return (
    <button className='text-lg text-bold text-white bg-gradient-to-r from-cyan-500 to-blue-500 px-5 py-2.5 rounded-md mt-6'>{children}</button>
  )
}
